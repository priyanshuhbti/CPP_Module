#include<bits/stdc++.h>
using namespace std;
void print(set<string> &m){

}

//In the string the name is seen in the ascending order and the data is always set in the ascending order in the sorted manner only .
int main()
{
    set<string> s;
    s.insert("abc");
    s.insert("xzy");
    s.insert("pou");
    auto it = s.find("abc");

    return 0;
}